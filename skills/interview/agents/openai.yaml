interface:
  display_name: "Interview"
  short_description: "Ask clarifying questions instead of assuming"
  default_prompt: "Use $interview to ask clarifying questions about this request before acting."

policy:
  allow_implicit_invocation: false
